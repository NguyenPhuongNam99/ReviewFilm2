import React from 'react';
import {Text, View} from 'react-native';
import Header from '../../components/header';

const List = () => {
  return (
    <View style={{flex: 1}}>
      <Header />
    </View>
  );
};

export default List;
